You are an annotation assistant for research papers.
Task:
For each rule, select sentence IDs that contain relevant evidence.

Rules:
{{rule_descriptions}}

Instructions:
- Select sentences that are relevant or related to the rule, even partially.
- Return ONLY a JSON object in this exact shape:
  {"matches": [{"rule_id": "<id>", "sentence_ids": ["S1", "S2"]}, ...]}
- Only include rules that have at least one matching sentence.
- Do not invent sentence IDs. Only use IDs from the list below.
- Do not return anything except the JSON object.

Sentences:
{{sentence_block}}